
import structures.Rectangle;

import java.util.List;

/*
class RTreeSearchEvaluator {
    private int diskAccessCount;

    public RTreeSearchEvaluator() {
        this.diskAccessCount = 0;
    }

    public List<Rectangle> searchWithEvaluation(RTreeNode root, Rectangle queryRectangle) {
        diskAccessCount = 0; // resetear contador
        long startTime = System.currentTimeMillis();

        List<Rectangle> results = search(root, queryRectangle);

        long endTime = System.currentTimeMillis();
        long elapsedTime = endTime - startTime;

        System.out.println("Tiempo de búsqueda: " + elapsedTime + "ms");
        System.out.println("Accesos a disco: " + diskAccessCount);

        return results;
    }

    private List<Rectangle> search(RTreeNode node, Rectangle queryRectangle) {
        diskAccessCount++; // Simulación de acceso a disco

        // ... (el mismo código de búsqueda que antes) ...
    }

    // ... (otros métodos y funciones auxiliares) ...
}

*/